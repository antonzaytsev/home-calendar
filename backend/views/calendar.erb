<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="refresh" content="600"> <!-- Refresh every 10 minutes -->
    <title>Home Calendar</title>
    <style>
        body {
            margin: 0;
            padding: 20px;
            font-family: Arial, sans-serif;
            background-color: #000;
            color: #fff;
            font-size: 18px;
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
        }

        .header h1 {
            font-size: 36px;
            margin: 0;
            font-weight: normal;
        }

        .settings-link {
            position: absolute;
            top: 20px;
            right: 20px;
            color: #007AFF;
            text-decoration: none;
            font-size: 24px;
            padding: 10px 15px;
            border: 1px solid #007AFF;
            border-radius: 5px;
        }

        .error {
            background-color: #8B0000;
            border: 1px solid #FF4444;
            border-radius: 5px;
            padding: 15px;
            margin: 20px 0;
            text-align: center;
            font-size: 20px;
        }

        .calendar-table {
            width: 100%;
            border-collapse: collapse;
            table-layout: fixed;
        }

        .calendar-table th {
            background-color: #333;
            padding: 15px 10px;
            border: 1px solid #555;
            text-align: center;
            font-size: 20px;
            font-weight: bold;
        }

        .calendar-table td {
            border: 1px solid #555;
            padding: 15px 10px;
            vertical-align: top;
            height: 400px;
            background-color: #1a1a1a;
        }

        .day-header {
            text-align: center;
            margin-bottom: 15px;
            font-size: 22px;
            font-weight: bold;
        }

        .day-number {
            font-size: 32px;
            margin-bottom: 10px;
        }

        .today {
            background-color: #007AFF !important;
            color: white;
        }

        .center-day {
            background-color: #2a2a2a !important;
            border: 2px solid #007AFF !important;
        }

        .event {
            background-color: #333;
            border-left: 4px solid #007AFF;
            padding: 8px;
            margin-bottom: 8px;
            border-radius: 3px;
            font-size: 16px;
            line-height: 1.3;
        }

        .all-day-event {
            border-left-color: #FF9500;
            background-color: #2a1f00;
        }

        .event-time {
            color: #999;
            font-size: 14px;
            margin-bottom: 5px;
        }

        .event-title {
            font-weight: bold;
            margin-bottom: 3px;
        }

        .event-location {
            color: #ccc;
            font-size: 14px;
            font-style: italic;
        }

        .no-events {
            color: #666;
            text-align: center;
            font-style: italic;
            margin-top: 20px;
        }

        .footer {
            text-align: center;
            margin-top: 20px;
            color: #999;
            font-size: 16px;
        }

        /* iPad specific optimizations */
        @media screen and (max-width: 1024px) {
            body { font-size: 16px; }
            .header h1 { font-size: 32px; }
            .calendar-table th { font-size: 18px; }
            .day-number { font-size: 28px; }
            .event { font-size: 14px; }
        }
    </style>
</head>
<body>
    <a href="/settings" class="settings-link">⚙️</a>

    <div class="header">
        <h1><%= @week_dates.first.strftime('%B %Y') %></h1>
    </div>

    <% if @error %>
    <div class="error"><%= @error %></div>
    <% end %>

    <table class="calendar-table">
        <thead>
            <tr>
                <% @week_dates.each do |date| %>
                <th><%= date.strftime('%A') %></th>
                <% end %>
            </tr>
        </thead>
        <tbody>
            <tr>
                <% @week_dates.each_with_index do |date, index| %>
                <td class="<%= 'today' if date == @today %> <%= 'center-day' if index == 3 %>">
                    <div class="day-header">
                        <div class="day-number"><%= date.strftime('%d') %></div>
                    </div>

                    <div class="day-events">
                        <% if @week_events[date] && @week_events[date].any? %>
                            <% @week_events[date].each do |event| %>
                            <div class="event <%= 'all-day-event' if event['all_day'] %>">
                                <div class="event-time"><%= format_event_time(event) %></div>
                                <div class="event-title"><%= event['summary'] %></div>
                                <% if event['location'] && !event['location'].empty? %>
                                <div class="event-location">📍 <%= event['location'] %></div>
                                <% end %>
                            </div>
                            <% end %>
                        <% else %>
                            <div class="no-events">No events</div>
                        <% end %>
                    </div>
                </td>
                <% end %>
            </tr>
        </tbody>
    </table>

    <div class="footer">
        <p>Last updated: <%= @today.strftime('%Y-%m-%d %H:%M') %> | Auto-refresh every 10 minutes</p>
    </div>
</body>
</html>
